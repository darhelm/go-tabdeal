diff --git a/errors.go b/errors.go
index c121587..dc1139e 100644
--- a/errors.go
+++ b/errors.go
@@ -42,7 +42,7 @@ type APIError struct {
 	Code       string
 	Message    string
 	Detail     string
-	StatusCode int
+	StatusCode uint16
 
 	// Map of all parsed key->values for inspection (similar to go-bitpin)
 	Fields map[string][]string
@@ -50,7 +50,7 @@ type APIError struct {
 
 // parseErrorResponse creates the most complete APIError possible.
 // It attempts all documented + undocumented patterns.
-func parseErrorResponse(statusCode int, respBody []byte) *APIError {
+func parseErrorResponse(statusCode uint16, respBody []byte) *APIError {
 	apiErr := &APIError{
 		StatusCode: statusCode,
 		Fields:     make(map[string][]string),
diff --git a/git-diff.txt b/git-diff.txt
new file mode 100644
index 0000000..e4c3c9c
--- /dev/null
+++ b/git-diff.txt
@@ -0,0 +1,21 @@
+diff --git a/types/marketInformation.go b/types/marketInformation.go
+index 0bc6c75..43fd1e0 100644
+--- a/types/marketInformation.go
++++ b/types/marketInformation.go
+@@ -1,7 +1,5 @@
+ package types
+ 
+-import "time"
+-
+ // Nobitex holds general market-wide configuration values,
+ // including supported currencies and precision settings.
+ type Nobitex struct {
+@@ -93,7 +91,7 @@ type OrderBook struct {
+ // timestamp, price, size, and direction.
+ type Trade struct {
+ 	// Time is the execution timestamp of the trade.
+-	Time time.Time `json:"time"`
++	Time int `json:"time"`
+ 
+ 	// Price is the executed trade price.
+ 	Price string `json:"price"`
